<%= form_with(model: quarter, local: true) do |form| %>
  <% if quarter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quarter.errors.count, "error") %> prohibited this quarter from being saved:</h2>

      <ul>
        <% quarter.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :zip_code %>
    <%= form.text_field :zip_code %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
